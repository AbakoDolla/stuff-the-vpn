/**
 * utils/index.ts
 * Fonctions utilitaires réutilisables.
 *
 * - generateVoucherCode() : Génération de codes uniques
 * - hashPassword()        : Hashage sécurisé des mots de passe
 * - formatBytes()         : Formatage des tailles de données
 * - parseDuration()       : Parsing des durées (ex: "30d", "1y")
 * - generateConfig()      : Génération des configs V2Ray/SSH
 *
 * TODO (Phase 3): Implémenter les utilitaires.
 */

export {};
