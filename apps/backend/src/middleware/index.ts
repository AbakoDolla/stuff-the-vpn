/**
 * middleware/index.ts
 * Middlewares Express globaux.
 *
 * - auth.ts       : Vérification du JWT, protection des routes
 * - rateLimiter.ts: Limitation du nombre de requêtes par IP
 * - logger.ts     : Logging des requêtes HTTP
 * - errorHandler.ts: Gestion centralisée des erreurs
 * - validate.ts   : Validation des corps de requêtes via Zod
 *
 * TODO (Phase 3): Implémenter chaque middleware.
 */

export {};
