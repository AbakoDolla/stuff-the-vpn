name: stuff_the_vpn
description: Application mobile VPN - Stuff The VPN
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # TODO (Phase 5): Ajouter les dépendances
  # http: ^1.1.0
  # shared_preferences: ^2.2.0
  # provider: ^6.0.5
  # go_router: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
