name: stuff_the_vpn
description: Application mobile VPN - Stuff The VPN
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_v2ray: ^1.0.9
  http: ^1.1.0
  shared_preferences: ^2.2.0
  provider: ^6.0.5
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
