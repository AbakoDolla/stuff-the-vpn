// features/voucher/screen.dart
// Écran : Voucher
//
// Activation et gestion des vouchers.
// - Saisie manuelle ou scan QR code
// - Historique des vouchers activés
//
// TODO (Phase 5): Implémenter l'écran complet.

import 'package:flutter/material.dart';

class VoucherScreen extends StatelessWidget {
  const VoucherScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Voucher')),
      body: const Center(
        child: Text('TODO: Implémenter Voucher'),
      ),
    );
  }
}
