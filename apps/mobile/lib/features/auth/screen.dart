// features/auth/screen.dart
// Écran : Auth
//
// Gère la connexion et l'inscription des utilisateurs.
// - Formulaire login/password
// - Connexion par code voucher direct
//
// TODO (Phase 5): Implémenter l'écran complet.

import 'package:flutter/material.dart';

class AuthScreen extends StatelessWidget {
  const AuthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Auth')),
      body: const Center(
        child: Text('TODO: Implémenter Auth'),
      ),
    );
  }
}
