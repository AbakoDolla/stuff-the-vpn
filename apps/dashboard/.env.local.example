# Backend URL used by server-side proxy routes
BACKEND_URL=https://vpnsxb.afrihall.com/api
# Public API URL used by browser-side code when direct API access is needed
NEXT_PUBLIC_API_URL=http://localhost:5000
