/**
 * DataTable.tsx
 * Composant : DataTable
 *
 * Tableau de données paginé et filtrable, réutilisable pour toutes les entités.
 *
 * TODO (Phase 4): Implémenter le composant.
 */

export function DataTable() {
  return <div>{/* TODO: DataTable */}</div>;
}
