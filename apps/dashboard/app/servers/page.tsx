/**
 * Page : Servers
 * 
 * TODO (Phase 4): Implémenter l'interface complète.
 * - Statut des serveurs VPN (online/offline, charge)
 * - Ajout et configuration de nouveaux serveurs
 */

export default function ServersPage() {
  return (
    <main>
      <h1>Servers</h1>
      {/* TODO (Phase 4): Implémenter le contenu */}
    </main>
  );
}
