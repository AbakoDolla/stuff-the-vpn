name: Build & Download APK

on:
  push:
    branches: [ main, sxb-vpn-redesign ]
    paths:
      - 'mobile/**'
      - 'apps/mobile/**'
      - '.github/workflows/build-apk.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'mobile/**'
      - 'apps/mobile/**'
      - '.github/workflows/build-apk.yml'
  workflow_dispatch:
    inputs:
      backend_url:
        description: 'Backend URL'
        required: false
        default: 'https://api.sxbvpn.afrihall.com'

permissions:
  contents: read
  actions: write

jobs:
  build-apk:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.0'
          channel: 'stable'
          cache: true

      - name: Setup Android SDK
        uses: android-actions/setup-android@v2

      - name: Fix JAVA_HOME
        run: |
          # Fix JAVA_HOME path if needed
          if [ ! -d "$JAVA_HOME" ]; then
            export JAVA_HOME="/opt/hostedtoolcache/Java_Temurin-Hotspot_jdk/17.0.19-10/x64"
          fi

      - name: Accept licenses
        run: yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true

      - name: Determine build directory
        id: build_dir
        run: |
          if [ -d "mobile" ]; then
            echo "dir=mobile" >> $GITHUB_OUTPUT
          elif [ -d "apps/mobile" ]; then
            echo "dir=apps/mobile" >> $GITHUB_OUTPUT
          else
            echo "dir=mobile" >> $GITHUB_OUTPUT
          fi

      - name: Install dependencies
        run: |
          cd ${{ steps.build_dir.outputs.dir }}
          flutter pub get

      - name: Ensure logo asset exists
        run: |
          mkdir -p ${{ steps.build_dir.outputs.dir }}/assets/images
          if [ -f logo.png ]; then
            cp logo.png ${{ steps.build_dir.outputs.dir }}/assets/images/logo.png
          elif [ -f apps/dashboard/public/logo.png ]; then
            cp apps/dashboard/public/logo.png ${{ steps.build_dir.outputs.dir }}/assets/images/logo.png
          fi

      - name: Determine Backend URL
        id: backend
        run: |
          if [ -n "${{ inputs.backend_url }}" ]; then
            echo "url=${{ inputs.backend_url }}" >> $GITHUB_OUTPUT
          else
            echo "url=https://api.sxbvpn.afrihall.com" >> $GITHUB_OUTPUT
          fi

      - name: Build APK (release)
        env:
          JAVA_HOME: /opt/hostedtoolcache/Java_Temurin-Hotspot_jdk/17.0.19-10/x64
        run: |
          cd ${{ steps.build_dir.outputs.dir }}
          flutter build apk --release \
            --dart-define=BACKEND_URL=${{ steps.backend.outputs.url }}

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: sxbvpn-release-apk-${{ github.run_number }}
          path: ${{ steps.build_dir.outputs.dir }}/build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30

      - name: Build debug APK for testing
        env:
          JAVA_HOME: /opt/hostedtoolcache/Java_Temurin-Hotspot_jdk/17.0.19-10/x64
        run: |
          cd ${{ steps.build_dir.outputs.dir }}
          flutter build apk --debug \
            --dart-define=BACKEND_URL=${{ steps.backend.outputs.url }}

      - name: Upload debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: sxbvpn-debug-apk-${{ github.run_number }}
          path: ${{ steps.build_dir.outputs.dir }}/build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 7
