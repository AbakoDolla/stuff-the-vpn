name: Build & Download APK

on:
  push:
    branches: [ main ]
    paths:
      - 'mobile/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'mobile/**'
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  build-apk:
    name: Build APK
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
          cache: true
          cache-dependency-path: mobile/pubspec.lock

      - name: Get Java path
        id: java
        run: echo JAVA_HOME= >> $GITHUB_OUTPUT

      - name: Install dependencies
        run: |
          cd mobile
          flutter pub get

      - name: Create assets directory and copy logo
        run: |
          mkdir -p mobile/assets/images
          cp apps/dashboard/public/logo.png mobile/assets/images/logo.png || echo "Logo not found"
          ls -la mobile/assets/images/

      - name: Update pubspec.yaml
        run: |
          if ! grep -q "assets:" mobile/pubspec.yaml; then
            echo '' >> mobile/pubspec.yaml
            echo '  assets:' >> mobile/pubspec.yaml
            echo '    - assets/images/' >> mobile/pubspec.yaml
          fi

      - name: Build APK
        env:
          JAVA_HOME: /opt/hostedtoolcache/Java_Temurin-Hotspot_jdk_jre/17.0.19-10/x64
        run: |
          cd mobile
          flutter build apk --debug 2>&1 || true
          ls -la build/app/outputs/flutter-apk/ 2>/dev/null || echo "No APK found"

      - name: Upload APK artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: sxbvpn-debug-apk
          path: mobile/build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 7
